<%= form_for @progress_update do |f| %>
    <!-- This line needs work -->
    <%= f.collection_select :user_goal_id , UserGoal.where(user_id: session[:user_id]) , :id , :goal_name   %>
    <%= f.text_area :content %>
    <%= f.submit 'Show your progress!' %>
    <% end %>