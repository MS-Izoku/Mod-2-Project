

<div class="container">
    <div class="container">
    <% @user.user_goals.each do |user_goal| %>
        <div class="bg-info shadow-lg rounded mb-2 mt-2 pt-3 pb-3 mb-2 mt-5">
            <div class="bg-white rounded mx-2">
                <hr>
                    <h2 class="text-center pt-3 pb-3"><%= link_to user_goal.goal.name, user_goal_path(user_goal) %></h2>
                <hr>
            </div>
        </div>
    <% end %>
    </div>
</div>




