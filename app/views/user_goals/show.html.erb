<h1>Hey <%= @usergoal.user.username %>! How is <%= @usergoal.goal.name.downcase %> going? </h1>

<div>
    
   
    <% if @usergoal.completion %>
        <h3>You CRUSHED it!!!</h3>
    <% else %>
        <h3>Keep working at it!</h3>
    <% end %>
    
    <h4>Frequency: <%= @usergoal.frequency.downcase %></h4>
</div>

<div>
<h4>Notes: <%= @usergoal.notes %></h4>
</div>


<% if logged_in? %>
    <%= link_to 'Edit Goal', edit_user_goal_path %>
    <%#= link_to 'Did you complete your goal?', complete_path(@usergoal) %>
    <%= link_to 'Did you complete your goal?', new_user_goal_progress_update_path(@usergoal) %>
    <%= render 'delete_button' %>
<% end %>
<br>
<%= link_to "Go Back", user_path(@usergoal.user) , class:"btn btn-info mt-1"%>
